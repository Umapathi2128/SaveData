<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.yugan.savedata">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/save"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon"

        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".view.MainActivity" android:windowSoftInputMode="adjustResize"/>
        <activity android:name=".add.AddActiviity" />
        <activity android:name=".KeygaurdActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>